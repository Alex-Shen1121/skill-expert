# Agent 技能管家发行

用于区分 Agent 技能管家的显示品牌、独立技术身份、上游项目与发布边界的统一语言。

## Language

**Upstream project**:
The external `xingkongliang/skills-manager` project, with its own decisions,
versions, releases, and update channel.

**Independent fork**:
The `Alex-Shen1121/skill-expert` project, which owns a release line distinct
from the upstream project even when it incorporates upstream changes.

**Agent 技能管家**：
独立 fork 面向用户展示的桌面应用名称，覆盖应用包、窗口、托盘、界面文案、诊断、下载说明与发布标题。
_避免使用_：Skill Expert、Skills Manager

**Skill Expert 技术身份**：
为兼容既有安装与发布链而保留的 `skill-expert` 仓库、CLI、包名、数据目录、Bundle ID、Updater 信任根和 Release 资产前缀；它不是当前应用显示名称。
_避免使用_：Agent 技能管家仓库、Agent 技能管家 CLI

**上游跟踪**：
对可信上游的完整变更进行审阅，只引入 Agent 技能管家需要的部分，同时保留独立的产品决策与发行线。
_避免使用_：上游同步、整包合并、自动合并

**Existing-installation import**:
A user-approved one-time copy of upstream product data into Agent 技能管家, not a
shared live data store between the products.
_Avoid_: Migration, shared library

**主分支**：
唯一接收产品变更和正式版本提交的 `main` 分支；所有开发变更都通过 `codex/*` PR 合入，上游评审分支是唯一自动化例外。
_避免使用_：生产分支、发布分支

**开发分支**：
从最新 `origin/main` 建立、通过轻量检查后以 PR 合入主分支的 `codex/*` 分支。
_避免使用_：直接提交分支

**上游评审分支**：
自动化生成上游变更审阅 PR 的固定 `upstream-tracking/main` 分支；它不是开发分支，也不会自动合入主分支。
_避免使用_：开发分支、自动同步分支

**发布授权**：
用户在当前请求中明确提出“发布新版本”或指定 `vX.Y.Z`；普通更新和测试打包均不构成发布授权。
_避免使用_：版本变化、main push

**发布准备 PR**：
只负责稳定版本号与双语更新日志归档的 `codex/release-vX.Y.Z → main` PR。
_避免使用_：Release PR、晋级 PR

**历史 release 分支**：
暂时保留但不再被代码、工作流、版本策略或正式发布依赖的旧 `release` 分支。
_避免使用_：发布源、生产分支

## 来源语言

**来源定位**：
不含访问凭证、用于找到一个 Skill 发布位置的地址；Git 来源由主机、仓库命名空间／名称和 Skill 子路径组成，本地来源由规范化绝对路径组成。
_避免使用_：原始 URL、下载地址、来源身份

**来源身份**：
跨安装、导入和更新稳定区分 Skill 来源的标识；Git 来源身份由来源类型与来源定位共同确定，传输协议、URL 凭证、跟踪分支和当前内容不属于来源身份。
_避免使用_：Skill 名称、内容哈希、来源 URL

**来源发布名称**：
来源内容声明的原始 Skill 名称，也是来源证据的一部分；库内消歧、目录后缀或用户别名不得改写它。
_避免使用_：库内别名、中央目录名、显示名称

**来源跟踪策略**：
指定后续检查和更新应跟随哪个分支或标签的来源规则；它附着于来源身份，但不是来源身份本身。
_避免使用_：来源身份、已确认修订

**来源证据**：
说明来源身份及其内容如何被确认的事实集合，包括跟踪策略、已确认修订、有效内容摘要、检查主体和检查时间。
_避免使用_：来源可信、仓库可访问

**来源信任依据**：
产品内置来源或用户明确授信所提供的来源判断，独立于来源是否可访问、结构是否有效或内容是否相同；只有存在这类依据时才使用“可信”。
_避免使用_：来源证据、连接成功、安全评分

**来源授信**：
用户对一个精确来源身份作出的持久、可撤销信任决定；它不扩展到整个主机、组织或仓库，也不批准跟踪策略变化、内容覆盖或文件删除。
_避免使用_：仓库授权、主机信任、更新批准

**来源身份冲突**：
不同来源身份试图复用同一中央 Skill 记录、目录或保留名称的状态；即使当前有效内容相同，也必须在任何写入前停止。
_避免使用_：内容冲突、同名 Skill、更新可用

**来源并存别名**：
用户为同名异源 Skill 明确指定、仅用于中央技能库区分两者的名称；它不改变来源发布的原始名称或来源身份，也不由产品自动生成数字后缀。
_避免使用_：Skill 重命名、来源名称、`-2` 后缀

**来源切换**：
用户明确选择让既有中央 Skill 原子地改为跟随另一个来源身份并采用其已验证内容；来源身份、跟踪策略、精确修订、有效内容和来源证据必须一起生效或一起保持原状。
_避免使用_：来源更新、重新安装、自动重定向

**来源切换计划**：
执行来源切换前绑定当前状态的不可变影响说明，包括新旧来源、内容变化、文件删除和已有部署影响；状态变化后，既有计划不再代表可执行意图。
_避免使用_：来源差异、更新预览、永久授权

**来源变更**：
既有 Skill 的来源身份或来源跟踪策略发生变化；同一身份与跟踪策略下只有已确认修订前进属于普通来源更新，不是来源变更。
_避免使用_：来源更新、内容变化、仓库刷新

**来源历史改写**：
来源身份与跟踪策略未变，但新修订不是上次已确认修订的后代，或者已确认标签移动到另一修订的状态；它使既有内容证据失效，不能作为普通来源更新继续。
_避免使用_：来源更新、来源变更、内容变化

**来源凭证**：
用于访问受限来源的令牌、密码或密钥；它不是来源定位、来源身份或来源证据，也不属于可同步或可导出的 Skill 元数据。
_避免使用_：来源 URL、来源参数、私有来源信息

**私有来源信息**：
可能暴露内部主机、仓库、Skill 名称或本地路径的来源定位信息；它可在本机及用户主动配置的备份中保留，但未经明确同意不得发送给其他第三方。
_避免使用_：来源凭证、公开来源

## 更新语言

**应用更新**：
Agent 技能管家应用本体从一个稳定版本升级到更高稳定版本的过程；它属于独立发行线，不包含任何 Skill 内容更新。
_避免使用_：自动更新、Skill 自动更新

**Skill 自动更新**：
用户已安装的 Skill 从各自来源升级到新修订的过程；它独立于 Agent 技能管家应用版本。
_避免使用_：应用更新、软件更新

**来源检查**：
读取一个明确来源快照并确认其来源修订与 Skill 有效内容摘要的操作；它只更新来源证据，不修改中央技能库内容或任何部署目标。
_避免使用_：检查更新、来源更新、更新完成

**中央更新**：
中央技能库原子采用已确认来源快照的 Skill 有效内容与来源证据；它不代表任何 Agent 或工作区目标已经对齐。
_避免使用_：来源检查、目标对齐、更新完成

**来源内容一致**：
来源检查确认来源修订对应的 Skill 有效内容与中央技能库相同；它可以推进来源证据，但不重写中央文件，也不代表部署目标已经验证。
_避免使用_：已更新、无更新、端到端更新完成

**目标对齐**：
让一个预期 Agent 或工作区目标采用中央技能库当前有效内容的操作；每个目标独立产生结果，一个目标失败不改变其他目标已经成立的事实。
_避免使用_：中央更新、来源更新、全部同步

**目标已修复**：
中央技能库内容未变化，但一个缺失、陈旧或错误的部署目标已重新对齐并通过验证；它不是中央更新或内容未变化。
_避免使用_：已更新、重新安装、目标未变化

**内容未变化**：
对本地或导入来源执行重新读取后，来源与中央技能库的 Skill 有效内容仍然相同；操作执行本身不能把该结果称为“已更新”。
_避免使用_：已更新、来源内容一致、目标已修复

**Skill 有效内容验证**：
按统一内容范围重新读取实际 Skill 目录，同时确认受支持的 `SKILL.md` 结构、文件字节、相对路径和适用平台的可执行权限；遍历、读取或权限检查错误都会使验证失败，且普通验证不执行 Skill 代码。
_避免使用_：内容哈希、脚本测试、命令成功

**端到端更新完成**：
来源检查、中央更新、全部预期目标对齐及其 Skill 有效内容验证均已成功；没有部署目标时，以中央技能库通过验证为完成边界。
_避免使用_：检查完成、中央已更新、批次结束

**更新部分成功**：
中央技能库已经采用并验证新内容，但至少一个预期目标失败、冲突或未开始；已成功事实继续保留，失败目标可独立重试。
_避免使用_：更新完成、更新失败、全部成功

**更新运行证据**：
一次检查或更新运行绑定的批次身份、起止时间、来源快照、中央内容前后摘要和逐目标结果；历史运行证据可继续展示，但不能冒充本轮新证据。
_避免使用_：当前状态、进度百分比、最后缓存

**后台更新完整成功**：
一次后台更新运行中的全部可处理项目均按同一更新契约完成或确认无变化；“上次尝试”不等于“上次完整成功”，部分或全部失败不能推进完整成功时间。
_避免使用_：后台任务结束、上次运行、定时器完成

**更新批次终态**：
一次批次在所有项目停止运行后的整体结果，只能是完成、部分完成、失败、需要确认或已停止之一；进度达到百分之百只表示项目均已进入终态。
_避免使用_：批次结束、调用成功、全部完成

**更新停止**：
用户请求更新运行不再调度新项目并取消可取消的在途工作；已经进入不可中断原子提交区的项目安全收尾后记录结果，已完成事实与未开始项目都继续保留。
_避免使用_：关闭进度窗口、强制终止、回滚批次

**目标验证证据**：
从实际部署目标读回的节点类型、预期指向、Skill 有效内容摘要、验证时间和错误；部署记录本身不是目标有效性的证明。
_避免使用_：部署记录、目标存在、同步状态

**失败目标重试**：
在中央技能库内容与来源证据仍和原更新运行一致时，只重新处理失败、冲突已解决或未开始的目标；证据已经变化时必须产生新的更新运行。
_避免使用_：重新更新、全部重试、强制覆盖

**Agent Skills 管理配置**：
用户可重复查看和调整哪些 Agent 能够直接管理中央技能库的产品配置；它与首次引导是否已关闭无关。设置页模块面向用户的固定标题为“Agent 管理 Skills”。
_避免使用_：二次设置、恢复提示

**Agent Skills 管理能力**：
通过把来自可信固定来源的 `manage-skills` 部署到指定 Agent，使该 Agent 可以直接管理中央技能库的能力。撤销该能力只撤销对应 Agent 的部署，不删除中央库中的 Skill。
_避免使用_：Agent 权限、删除管理 Skill

**Agent Skills 管理目标**：
可以获得 Agent Skills 管理能力的已启用 Agent，以及仍保留该能力部署记录的已停用或已卸载 Agent。从未安装且没有部署记录的 Agent 不属于此集合。
_避免使用_：所有 Agent、支持的 Agent

**Agent Skills 首次引导**：
Dashboard 上一次性介绍 Agent Skills 管理能力的入口；至少一个 Agent 部署成功或用户主动关闭后结束，但不影响永久可用的 Agent Skills 管理配置。
_避免使用_：Agent Skills 管理配置、永久入口

**保留管理 Skill**：
产品内置、固定使用名称 `manage-skills`、且有效内容由当前 Agent 技能管家应用版本批准的特殊 Skill；只有来源身份、精确修订和有效内容摘要均匹配该版本证据的实例才能提供 Agent Skills 管理能力。
_避免使用_：普通 manage-skills、用户授信 Skill、管理插件

**管理 Skill 来源冲突**：
中央技能库的保留名称 `manage-skills` 被不符合产品内置来源身份与来源证据的 Skill 占用；普通来源授信不能使其获得 Agent Skills 管理能力，也不能自动替换它。
_避免使用_：已安装管理 Skill、可自动修复的同名 Skill

**可检查更新 Skill**：
来源仍可追踪的 Skill，包括直接 Git 来源、skills.sh 来源，以及保留有效原始路径的本地或导入来源；失去来源的本地副本不属于此集合。
_避免使用_：所有 Skill、有可用更新

**Skill 有效内容**：
参与来源更新判定与差异展示的 Skill 文件内容及可执行权限；不包含版本控制元数据、系统缓存和编译缓存等管理或派生文件。
_避免使用_：来源仓库修订、全部目录内容

**已对齐来源修订**：
最近一次确认其 Skill 有效内容与中央技能库当前内容一致的来源仓库修订；内容一致时，检查更新可以推进该修订而不改写 Skill 文件。
_避免使用_：最初安装版本、远端最新修订

**检查全部**：
面向整个中央技能库检查所有可检查更新 Skill 的前台批量操作，不受当前 Preset、搜索或筛选条件限制；它只刷新更新状态和已对齐来源修订，不修改 Skill 内容。单个来源失败只影响依赖该来源的 Skill；停止会立即阻止后续调度并取消可取消的在途 Git 网络请求，保留已完成结果，停止请求被接受后进度窗口可立即关闭。
_避免使用_：检查当前结果、全部更新

**全部更新**：
从中央技能库已缓存的有可用更新 Skill 中选择并明确确认后执行更新的前台批量操作，不受当前 Preset、搜索或筛选条件限制。其停止与进度窗口语义与检查全部一致，但仍不自动批准文件删除。
_避免使用_：检查全部、Skill 自动更新

**有可用更新**：
来源中的 Skill 有效内容与中央技能库当前内容不同的缓存状态，也是技能库中的视图条件；无论差异来自来源前进还是中央技能库修改，显式更新都会用来源内容替换当前内容，仅来源仓库修订变化而内容一致时视为已是最新。该语义统一适用于直接 Git、skills.sh、本地和导入来源，切换视图条件不会发起联网检查或执行更新。
_避免使用_：检查更新、全部更新

**来源仓库**：
直接 Git Skill 解析后的克隆仓库身份；不包含来源分支、Skill 子目录、skills.sh 来源或技能库备份仓库。筛选入口按规格使用“Git 仓库”“Git 倉庫”和“Git repositories”界面标签，以强调其仅适用于直接 Git 来源；这些标签不是独立领域概念。
_避免使用_：分支来源、Skill 子目录、技能库备份仓库

## 备份与恢复语言

**可移植中央技能库状态**：
可跨设备恢复的中央 Skill 有效内容与稳定身份，以及标签、Preset、启用关系、无凭证来源定位和必要协议元数据；它不包含来源凭证、本机绝对路径、Agent 检测状态、实际部署目标或旧健康缓存。
_避免使用_：整机备份、应用设置、Agent 环境

**本机恢复点**：
只存在于当前设备、用于撤销本机操作的中央技能库快照；它不能抵御当前设备或磁盘丢失，因此不属于远端备份证据。
_避免使用_：备份快照、远端备份、可恢复备份

**远端备份快照**：
快照引用、提交和所需内容对象已通过远端往返验证，并能由另一设备重新取得的可移植中央技能库状态。
_避免使用_：本地 tag、远端 URL、备份配置

**恢复就绪度**：
远端备份快照在引用与对象完整性、元数据结构、Skill 有效内容、排除清单、来源可用性和恢复计划生成方面的证据集合；其结果分为可恢复、部分可恢复、需关注或已阻塞。
_避免使用_：快照数量、仓库已连接、已备份

**备份同步状态**：
当前中央技能库与备份远端之间的本地改动、远端可达性、领先／落后、推送结果和待裁决冲突；它与历史快照的恢复就绪度是两个独立维度。
_避免使用_：恢复就绪度、已备份、快照状态

**部分可恢复**：
远端备份快照中的中央内容可以恢复，但存在已明确排除的内容、失效来源、缺失凭证或不能自动重建的部署目标。
_避免使用_：恢复失败、已阻塞、完全可恢复

**恢复演练**：
从远端备份快照在隔离的临时中央库、索引和部署目标中完成真实重建与验证的非破坏性运行；它不修改当前中央库、真实 Agent 目录或远端仓库。
_避免使用_：恢复预览、真实恢复、Git 完整性检查

**轻量远端回读**：
每次成功备份后从远端确认快照引用、提交和所需内容对象已经到达的验证；它不能替代完整恢复演练。
_避免使用_：推送成功、本地校验、恢复演练

**恢复演练证据**：
一次恢复演练绑定的远端快照、验证范围、结果、耗时和时间；备份协议、应用结构或备份范围变化会使既有证据失效，稳定期间超过三十天则降为需关注。
_避免使用_：上次恢复、快照时间、永久有效

**恢复计划**：
真实恢复前绑定目标快照与当前中央状态的不可变影响说明，列出将新增、覆盖、删除、保留、排除、无法恢复或需要重新授权的项目，以及恢复后不会自动执行的部署建议。
_避免使用_：恢复确认框、快照摘要、恢复日志

**完整本机安全点**：
真实恢复前对恢复可能改写的中央内容与索引状态所作的本机验证副本，包括受跟踪、未跟踪、忽略和因超限排除的内容；它不包含或改写系统来源凭证。
_避免使用_：本机恢复点、Git tag、远端备份快照

**中央技能库恢复完成**：
目标快照已应用，中央索引已重建，全部纳入范围的 Skill 内容、稳定身份、标签、Preset 和来源元数据均已读回验证；它不表示当前设备的 Agent 部署或来源凭证已经恢复。
_避免使用_：恢复完成、全部恢复、部署完成

**恢复后部署计划**：
中央技能库恢复完成后，根据当前设备实际 Agent 环境生成、等待用户确认的目标部署建议；它不属于中央技能库恢复事务，也不能自动应用。
_避免使用_：自动重新部署、恢复目标、恢复完成

**来源待授权**：
中央 Skill 内容已经恢复，但当前设备缺少访问其私有来源所需凭证的状态；它阻止来源检查与更新，不阻止用户使用中央内容或明确部署该内容。
_避免使用_：来源丢失、恢复失败、凭证已恢复

**恢复受阻**：
真实恢复失败且完整本机安全点也未能通过回滚验证的保护状态；所有自动写入暂停，安全点、失败状态和诊断证据同时保留，直到用户确认一份完整状态。
_避免使用_：恢复失败、自动回滚、继续运行

## 计数与数据新鲜度语言

**指标身份**：
跨页面稳定定义一个计数的对象、作用域、过滤条件和聚合方式；相同指标身份必须引用同一证据批次，页面需要不同集合时必须使用不同名称。
_避免使用_：显示名称、页面数字、统计卡

**中央 Skill 数**：
按中央技能库稳定 Skill 身份去重后的对象数量，不受页面搜索、Preset 或工作区筛选影响。
_避免使用_：Skills 总数、磁盘副本数、当前结果数

**部署关系数**：
中央 Skill 与一个预期 Agent 或工作区目标之间的关系数量；关系记录存在不代表目标磁盘副本有效。
_避免使用_：已部署 Skill 数、磁盘副本数、已验证对齐副本数

**磁盘副本数**：
指定 Agent 或工作区目标路径中实际存在的 Skill 副本数量；它不说明副本是否受管、身份是否匹配或内容是否与中央库对齐。
_避免使用_：已部署 Skill 数、中央 Skill 数、已同步数

**已验证对齐副本数**：
身份与 Skill 有效内容都由当前有效证据确认和中央技能库一致的磁盘副本数量。
_避免使用_：磁盘副本数、部署关系数、同步覆盖

**工作区唯一 Skill 数**：
按中央 Skill 与工作区副本身份匹配契约形成的唯一 Skill 组数量；它不按目录名或显示名称自行猜测，也不等于 Agent 展开副本数。
_避免使用_：项目 Skill 数、磁盘副本数、Agent 展开副本数

**Agent 展开副本数**：
同一工作区 Skill 在不同 Agent 目标中分别计数后的副本总量。
_避免使用_：工作区唯一 Skill 数、中央 Skill 数、部署关系数

**当前结果数**：
当前搜索、筛选或页面范围内可见对象的数量；它不能替代对应指标的全量总数。
_避免使用_：总数、中央 Skill 数、筛选前数量

**计数证据状态**：
一个指标当前只能处于加载中、已验证、已陈旧、刷新失败或不可用／不适用；只有成功读取精确对象集合后，数值 `0` 才表示真实空集合。
_避免使用_：默认值、缓存命中、加载结果

**计数本地回退**：
主指标无法刷新时，从另一种本地对象集合得到、以不同指标名称明确展示的替代数字；它不能覆盖主指标或沿用主指标名称。
_避免使用_：旧值、估算值、静默回退

**工作区 Skill 组启用状态**：
一个工作区唯一 Skill 组相对全部适用 Agent 副本的启用程度，只能是全部启用、部分启用或未启用；组内任一副本启用不能代表整个组全部启用。
_避免使用_：项目启用数、任一启用、Agent 展开副本状态

**支持的 Agent**：
Agent 技能管家具有适配定义的 Agent 类型，不表示当前设备已经安装、检测或启用它。
_避免使用_：已检测 Agent、已启用 Agent、已接入 Agent

**已检测 Agent**：
当前设备已经验证存在对应应用、命令或目标目录的 Agent。
_避免使用_：支持的 Agent、已启用 Agent、已接入 Agent

**已启用 Agent**：
已检测且用户允许 Agent 技能管家管理其 Skills 的 Agent；启用不表示已经存在任何部署目标。
_避免使用_：已检测 Agent、已有部署 Agent、已接入 Agent

**已有部署 Agent**：
至少存在一个已经通过目标验证的 Skill 部署的 Agent；部署关系记录存在本身不足以进入此集合。
_避免使用_：已启用 Agent、部署关系数、已接入 Agent

**自定义 Agent**：
用户创建的 Agent 适配定义，无论当前设备是否检测到对应目标。
_避免使用_：已检测 Agent、第三方 Agent、未知 Agent

## 版本通道语言

**稳定源码版本**：
源码全部版本副本共同使用的 `x.y.z`；普通 PR 保持不变，只有发布准备 PR 才更新。
_避免使用_：开发序号、预发布版本

**正式发布提交**：
发布准备 PR 合入后位于 `main` 的精确提交，也是正式工作流唯一允许打包和标记的源码身份。
_避免使用_：最新 main、release HEAD

**正式发布工作流**：
显式绑定正式发布提交、一次构建四平台生产资产并在全部回验通过后公开 Latest 的手动工作流。
_避免使用_：候选晋级、tag push

**正式构建来源证明**：
`build-provenance.json`。证明公开资产来自正式发布提交和 `.github/workflows/release.yml`。
_避免使用_：候选来源证明、晋级证明

**应用安装包**：
供用户按操作系统和架构直接下载、安装或运行的桌面应用分发文件。
_避免使用_：Updater 包、CLI 包、全部 Release 附件

**应用更新资产**：
供 Agent 技能管家发现、验证并安装更高稳定版本的机器消费文件；它们是应用分发的一部分，但不是用户安装包。
_避免使用_：CLI 资产、Skill 自动更新资产

**公开发布资产**：
正式 Release 主动上传的应用安装包、应用更新资产、完整性清单和正式构建来源证明；不包含独立 CLI 下载或单独公开的 Updater 签名文件。
_避免使用_：所有构建产物、GitHub 自动生成的源码归档

**Draft 临时资产**：
只在不可见 Draft 中短暂保存、对应四个平台实际更新入口，并在生成应用更新元数据后、来源证明和公开前删除的独立 Updater 签名文件；独立 CLI 不属于此集合且不上传。
_避免使用_：内部资产、公开发布资产

**手工测试包**：
由独立手工入口生成、带 `manual-test-package` 与 `promotable: false` 标记的测试资产；默认只构建 macOS arm64，无论平台数量都不能转为正式 Release。
_避免使用_：正式版本、可发布包

## Compatibility-only legacy markers

The serialized backup protocol retains `.skills-manager` metadata paths,
`refs/skills-manager/*` hidden refs, and the `created_by: "skills-manager"`
schema marker so existing backups remain readable. These strings are protocol
compatibility identifiers, not user-facing product or repository identities.
Historical changelog entries remain unchanged as records of shipped releases.

## 工作树基线语言

**开发集成分支**：
远端 `origin` 的默认分支 `main`。所有实现 worktree 必须从首次校验时最新的 `origin/main` 建立。
_避免使用_：`master`、生产分支

**工作树基线**：
由公开仓库 CLI 读取的 Git 事实集合，包括主工作目录、Git common directory、全部 linked worktree、开发集成分支关系、当前工作区状态和远端刷新状态。
_避免使用_：审查页差异、未提交修改总数

**实现基线**：
实现 worktree 首次通过 preflight 时记录的 `origin/main` SHA。它保存在 worktree-local Git 配置中；后续远端前进不会改写该起点。
_避免使用_：本地 main 当前值、latest main

**本地 main 恢复点**：
安全同步前创建的本地 `codex/local-main-recovery-<date>` 分支，以及工具生成的完整性元数据和可选 tracked 内容快照。它没有 upstream，也不会自动 push。
_避免使用_：备份 tag、远端恢复分支

**已验证 recovery**：
分支、完整工具元数据、计划身份、引用与可选快照均通过同一验证契约的本地 main 恢复点。只有这种恢复点可进入自动同步。

**旧式未验证 recovery**：
符合恢复分支命名但缺少有效工具元数据的人工或旧流程恢复点。诊断将其标记为 `legacy/unverified`，只能人工审计，不能用于自动同步。

## 品牌语言

**角色标识**：
以用户提供的棕色卷发、星形元素 Q 版人物为核心的品牌识别形象。
_避免使用_：S 人物、S 吉祥物

**角色 App 图标**：
由角色标识构成的唯一产品身份图标，覆盖所有非系统状态区的应用、安装、启动、品牌和平台资产，不因尺寸缩小而替换图形。
_避免使用_：小尺寸技能核心图标、混合应用图标

**星际技能管理员**：
角色标识在 Agent 技能管家品牌中的身份，代表对 Skills 的收集、整理与调度。
_避免使用_：技能召唤师、纯角色头像

**技能核心**：
由角色星球灯棒演化而来的品牌道具，以透明星球、粉色握柄和星形核心表达被收集与调度的 Skills。
_避免使用_：麦克风、普通灯棒

**技能核心符号**：
技能核心在系统状态区中的极简图形，以星形核心与单条星环表达常驻的应用状态；它不属于角色 App 图标体系。
_避免使用_：小尺寸 App 图标、旧 S 标识

**系统状态区图标**：
操作系统提供的常驻状态入口，包括 macOS 菜单栏和 Windows、Linux 系统托盘；这是唯一使用技能核心符号的产品表面。
_避免使用_：窗口侧边栏图标、Finder 图标、任务栏应用图标

## 窗口与常驻状态语言

**主窗口**：
Agent 技能管家唯一承载桌面内容的窗口；隐藏、最小化和退出是三种不同状态。
_避免使用_：新窗口、应用进程

**窗口恢复**：
应用进程仍在运行时，来自 Dock、系统状态区或再次启动的打开请求显示、取消最小化并聚焦既有主窗口；它不会创建第二个窗口或第二个应用实例。
_避免使用_：重新启动、打开新窗口

**关闭到托盘**：
启用系统状态区图标时，关闭按钮隐藏主窗口并保持应用进程运行；macOS 可同时隐藏运行中的 Dock 状态，但来自固定 Dock 图标、Finder 或 Spotlight 的再次打开仍属于窗口恢复。它不等于退出应用。
_避免使用_：关闭应用、退出

**旧 S 标识**：
上一代版本使用的 S 形品牌符号；新一代图标不以保留该符号为约束。
_避免使用_：新角色标识
